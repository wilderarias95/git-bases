//Learning the git bases 
//we make our first tracking files, that's awesome.
//we modified the files to see the changes did it, and not staged with the command diff.
changed the core.editor
we created a new branch, called testing we are going to see this with git log --oneline --decorate --graph --all